

//enum values always start at 0 in Ts you can let them start with a default value

//enums also lets us add extres security to our types by allowing us to restrict our types.

const enum SeatChoic {
     UPPPER = "upper",
     LOWER = 2,
     MIDDLE 
}

const rkseat = SeatChoic.MIDDLE

